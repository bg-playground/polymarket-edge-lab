# Milestone 3G readiness

The initial strict pre-event implementation is complete enough for CI and empirical validation.

Implemented components:

- frozen pre-event prediction protocol;
- FIFO reconstruction with pre-target account-state snapshots;
- deterministic same-timestamp ordering via fill sequence;
- target-excluding rolling activity and inventory state;
- causal BTC alignment and provenance audit;
- frozen transparent/HGB model comparisons;
- untouched July 10–16 external advancement gate;
- deterministic unit tests;
- reproducible discovery/external GitHub Actions workflow.

PR results must remain unmerged until ordinary CI and the empirical Stage 3G workflow are inspected.